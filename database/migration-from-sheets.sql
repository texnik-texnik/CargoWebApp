-- =====================================================
-- MIGRATION SCRIPT: Google Sheets → Supabase
-- =====================================================
-- Этот скрипт помогает перенести данные из Google Sheets
-- =====================================================

-- 1. Экспорт данных из Google Sheets в CSV
--    - Откройте Google Sheets с пользователями
--    - File → Download → CSV
--    - Сохраните как users.csv
--
-- 2. Загрузите CSV в Supabase
--    - Откройте Table Editor
--    - Нажмите "Insert" → "Import CSV"
--    - Или используйте SQL ниже:

-- =====================================================
-- USERS - Пример импорта
-- =====================================================
-- Замените данные на ваши из Google Sheets

INSERT INTO users (telegram_id, client_id, name, phone, lang, history) VALUES
('123456789', 'KH-001', 'Ахмад Рахимов', '+992901234567', 'ru', 'TRACK001,TRACK002,TRACK003'),
('987654321', 'KH-002', 'Фирдавс Каримов', '+992912345678', 'tj', 'TRACK004,TRACK005'),
('456789123', 'KH-003', 'Иван Петров', '+992923456789', 'ru', 'TRACK006')
ON CONFLICT (telegram_id) DO NOTHING;

-- =====================================================
-- TRACKS - Пример импорта
-- =====================================================

INSERT INTO tracks (code, status, notes) VALUES
('TRACK001', 'intransit', 'Груз в пути, ожидается прибытие на склад'),
('TRACK002', 'warehouse', 'На складе в Душанбе, готов к выдаче'),
('TRACK003', 'border', 'На границе, оформление'),
('TRACK004', 'waiting', 'Ожидает отправки из Китая'),
('TRACK005', 'delivered', 'Доставлен клиенту'),
('TRACK006', 'received', 'Получен от поставщика')
ON CONFLICT (code) DO NOTHING;

-- =====================================================
-- Проверка импорта
-- =====================================================

-- Посчитать количество пользователей
SELECT COUNT(*) as total_users FROM users;

-- Посчитать количество треков по статусам
SELECT status, COUNT(*) as count 
FROM tracks 
GROUP BY status 
ORDER BY count DESC;

-- =====================================================
-- После импорта обновите связи
-- =====================================================

-- Если у вас есть таблица соответствия пользователей и треков
-- обновите поле history в users

-- Пример:
-- UPDATE users 
-- SET history = 'TRACK001,TRACK002,TRACK003'
-- WHERE telegram_id = '123456789';

-- =====================================================
-- ГОТОВО!
-- =====================================================
-- После успешного импорта:
-- 1. Проверьте данные в Table Editor
-- 2. Протестируйте приложение локально
-- 3. Задеплойте на Vercel
